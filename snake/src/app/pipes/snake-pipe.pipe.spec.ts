import { SnakePipePipe } from './snake-pipe.pipe';

describe('SnakePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SnakePipePipe();
    expect(pipe).toBeTruthy();
  });
});
